<fieldset><legend>Tratti della Discendenza</legend>
  {{#each system.tratti as |t|}}
    <div class="dh-priv-item">
      <strong>{{t.nome}}</strong>
      <div class="dh-desc">{{t.descrizione}}</div>
    </div>
  {{else}}
    <em class="dh-empty">Nessun tratto.</em>
  {{/each}}
</fieldset>
