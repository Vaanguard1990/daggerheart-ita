<div class="dh-grid-2">
  <label>Quantità <input type="number" name="system.quantita" value="{{system.quantita}}" min="0"/></label>
  <label>Usi (corrente / max)
    <span style="display:flex; gap: .25rem;">
      <input type="number" name="system.usi.value" value="{{system.usi.value}}" min="0"/>
      <input type="number" name="system.usi.max" value="{{system.usi.max}}" min="1"/>
    </span>
  </label>
</div>
